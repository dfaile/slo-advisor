# Core dependencies for SLO Advisor
openai>=1.0.0
pytest>=7.0.0
pytest-mock>=3.10.0

# Optional: For accurate token counting (recommended)
tiktoken>=0.5.0

# Optional: For DOCX output if needed in the future
# python-docx>=0.8.11

